/**
 * tools/generate-llms.js
 * Placeholder para evitar fallos en build
 */
try {
  // Placeholder: no-op
  // Escribe lógica aquí si necesita generar assets/llms
} catch (e) {
  console.error(e);
  process.exit(1);
}
